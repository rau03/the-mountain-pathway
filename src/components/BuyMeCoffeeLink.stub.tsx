import type { CSSProperties, ReactNode } from "react";

type BuyMeCoffeeLinkProps = {
  className?: string;
  textClassName?: string;
  iconClassName?: string;
  text?: string;
  style?: CSSProperties;
  defaultColor?: string;
  hoverColor?: string;
  icon?: ReactNode;
  showText?: boolean;
};

export default function BuyMeCoffeeLink(_props: BuyMeCoffeeLinkProps) {
  return null;
}
